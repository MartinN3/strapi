{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "EnvSettings.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cloudinary-config",
      "handler": "EnvSettings.getCloudinaryConfig",
      "config": {
        "policies": []
      }
    }
  ]
}
